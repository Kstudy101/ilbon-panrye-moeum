# 일본 행정서사 판례·용어 정리 저장소

일본 행정서사(行政書士) 시험 공부용 판례·용어 정리 저장소. 사용자가 단어를 보내면 아래 규칙에 따라 구분·정리·저장한다.

## 입력 구분 규칙

사용자가 단어만 보내면 먼저 **판례인지 용어인지 구분**한다.

- **판례**: 「〜事件」「〜訴訟」「〜判決」로 끝나는 이름, 판결 인용 표기(最大判昭52.7.13 등), 널리 알려진 판례 별칭 → 판례 워크플로
- **용어**: 법률 개념·제도·원칙·학설 명칭(예: 私人間効力, 目的効果基準, LRA基準) → 용어 워크플로
- 애매하면 사용자에게 확인한다.

## 판례 워크플로

1. `<과목>/<사건명>.md` 상세 파일 작성 (현재 과목 폴더: `헌법/`. 다른 과목이면 폴더 신설)
2. 파일 구조는 기존 판례 파일과 동일하게: 제목 → 메타 표(인용/사건번호/사건명/판례집/결론/과목) → `## 사안` → `## 쟁점` → `## 판시` → `## 시험 포인트` → `## 후속·관련` → `## 원문·참고`
3. [리스트.md](리스트.md) 표에 한 줄 추가 (# / 별칭 / 인용 / 과목 / 쟁점 / 결론 / 상세 링크)
4. 커밋: 「○○ 사건을 추가한다.」

## 용어 워크플로

1. 채팅으로 해설 출력: 행정서사 시험 관점의 개념 정의 → 학설·판례 입장(최고재 입장 명시) → 관련 필수 판례(사건명·인용·결론) → 💡 수험 팁(오답 포인트·암기 요령)
2. `용어/<용어명>.md` 파일 저장. 구조: 제목 → 메타 표(읽기/과목/한줄 정의/통설·판례) → `## 개념` → `## 학설`(해당 시) → `## 관련 판례`(표) → `## 💡 수험 팁`
3. [용어리스트.md](용어리스트.md) 표에 한 줄 추가
4. 커밋: 「○○ 용어를 추가한다.」

## 판례집 웹 책 (Artifact)

모든 판례·용어를 어디서든(폰 포함) 볼 수 있는 웹 책으로 발행한다.

- 빌드: `node tools/build_book.mjs` → 저장소 루트에 `book.html` 생성 (git 무시 대상)
- 발행: Artifact 도구로 `book.html`을 아래 URL에 재발행(`url` 파라미터에 기존 URL 전달, favicon ⚖️ 유지)
- **판례·용어를 추가·수정할 때마다 빌드 후 재발행까지가 한 세트다.**

### Artifact URL

- https://claude.ai/code/artifact/0d8e4e0a-a109-498a-8919-070f5213fc61

## 기타 규칙
