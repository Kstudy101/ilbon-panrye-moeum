# 일본 행정서사 판례·용어 정리 저장소

**일본** 행정서사(行政書士) 시험 공부용 판례·용어 정리 저장소다 (한국 행정사 아님). 사용자가 단어를 보내면 아래 규칙에 따라 구분·정리·저장한다.

## 입력 구분 규칙

사용자가 단어만 보내면 먼저 **판례인지 용어인지 구분**한다.

- **판례**: 「〜事件」「〜訴訟」「〜判決」로 끝나는 이름, 판결 인용 표기(最大判昭52.7.13 등), 널리 알려진 판례 별칭 → 판례 워크플로
- **용어**: 법률 개념·제도·원칙·학설 명칭(예: 私人間効力, 目的効果基準, LRA基準) → 용어 워크플로
- 애매하면 사용자에게 확인한다.

## 조사 원칙

- 일본 판례·용어의 내용은 **일본 웹 출처**에서 확인한다: 裁判所 판례검색(courts.go.jp), e-Gov 法令検索, 일본어 위키백과 등. 확인이 필요하면 research 에이전트를 사용한다.
- 각 판례 파일의 「원문·참고」에는 일본어 출처 링크를 단다.
- 한국 비교자료는 한국 출처(대법원 종합법률정보, casenote.kr, 국가법령정보센터 등)에서 확인한다.
- 출처로 확인 안 된 사실은 적지 않는다.

## 판례 워크플로

1. `<과목>/<사건명>.md` 상세 파일 작성 (현재 과목 폴더: `헌법/`. 다른 과목이면 폴더 신설)
2. 파일 구조는 기존 판례 파일과 동일하게: 제목 → 메타 표(인용/사건번호/사건명/판례집/결론/과목) → `## 사안` → `## 쟁점` → `## 판시` → `## 시험 포인트` → `## 후속·관련` → `## 원문·참고`
3. [리스트.md](리스트.md) 표에 한 줄 추가 (# / 별칭 / 인용 / 과목 / 쟁점 / 결론 / 상세 링크)
4. 빌드·배포 (아래) 후 커밋: 「○○ 사건을 추가한다.」

## 용어 워크플로

1. 채팅으로 해설 출력: 행정서사 시험 관점의 개념 정의 → 학설·판례 입장(최고재 입장 명시) → 관련 필수 판례(사건명·인용·결론) → 💡 수험 팁(오답 포인트·암기 요령)
2. `용어/<용어명>.md` 파일 저장. 구조: 제목 → 메타 표(읽기/과목/한줄 정의/통설·판례/한국 비교 링크) → `## 개념` → `## 학설`(해당 시) → `## 관련 판례`(표) → `## 💡 수험 팁` → `## 🎯 요점·예시`
   - 「요점·예시」는 초심자용: **요점**(전문용어를 풀어쓴 쉬운 한국어 1~2문장)과 **예시**(A·B 등 구체적 인물·상황이 등장하는 생활 예시 1~3문장, 파일 내용에 근거) 두 항목의 리스트로 쓴다
3. [용어리스트.md](용어리스트.md) 표에 한 줄 추가
4. 빌드·배포 (아래) 후 커밋: 「○○ 용어를 추가한다.」

## 한국 비교 워크플로 ⏸ 잠정 중단 (2026-08-23~)

**현재 중단 상태다.** 판례·용어를 추가할 때 한국 비교자료는 만들지 않는다. 기존 `한국비교/` 파일과 링크는 그대로 두고, 신규 용어·판례의 메타 표에는 「한국 비교」 행을 넣지 않는다. 재개 지시가 있으면 아래 절차를 되살린다.

<details>
<summary>중단된 절차 (재개용)</summary>

일본 판례·용어를 추가할 때, **대응하는 한국 법리·판례를 조사해 별도 저장**한다 (나중에 한일 비교자료로 쓰기 위함). 일본 자료와 섞지 않는다.

1. `한국비교/<주제명>.md` 작성. 구조: 제목 → 메타 표(대응(일본 항목 링크)/인용/과목/결론) → `## 한국의 법리` → `## 대표 판례` → `## 일본과의 비교` → `## 원문·참고`
2. 파일명과 제목(`# `)은 동일하게 (책 내부 링크가 제목 기준으로 연결됨)
3. 대응하는 일본 파일의 메타 표에 `| 한국 비교 | [링크] |` 행을 추가해 상호 연결
4. [한국비교리스트.md](한국비교리스트.md) 표에 한 줄 추가
5. 대응할 한국 사례가 마땅히 없으면 생략하고 사용자에게 그 사실을 알린다

</details>

## 빌드·배포 (Cloudflare Pages)

웹 책은 Cloudflare Pages로 서빙한다: **https://ilbon-panrye-moeum.pages.dev/**

- 리스트 갱신: `node tools/update_lists.mjs` → [용어리스트.md](용어리스트.md)의 「상세」 열을 실제 존재하는 용어 파일로 자동 채우고, 아직 없는 용어를 보고한다 (이미 채워진 칸과 「한국 비교」 열은 건드리지 않음)
- 빌드: `node tools/build_book.mjs` → 저장소 루트에 `index.html` 생성 (**커밋 대상**)
- 배포: 커밋 후 `git push` → GitHub(Kstudy101/ilbon-panrye-moeum) → Cloudflare Pages 자동 배포
- **판례·용어·비교자료를 추가·수정할 때마다 빌드 → 커밋 → 푸시까지가 한 세트다.**
- (참고) 구버전 Artifact 뷰: https://claude.ai/code/artifact/0d8e4e0a-a109-498a-8919-070f5213fc61 — 더 이상 기본 갱신 대상 아님

## 텔레그램 공부 알리미

`.github/workflows/telegram-study-alerts.yml`이 20분마다 실행되어, 매 실행마다 판례 OX 퀴즈 5건 + 용어 카드 5건(총 10개 메시지)을 텔레그램으로 발송한다. 용어 카드는 압축 요약이 아니라 개념·학설·관련 판례·수험 팁·요점/예시를 포함한 상세 버전이다.

- **설정**: `tools/telegram/config.json` — `enabled`(토글), `startHour`/`endHour`/`timezone`(발송 시간대), `quizBatchSize`/`termBatchSize`(회당 발송 개수), `reviewRatio`(복습 비율), `messageDelayMs`(메시지 간 간격), `termBodyMaxChars`(용어 카드 본문 글자수 상한, 텔레그램 4096자 제한 방어). 이 파일만 고쳐 커밋하면 워크플로 수정 없이 즉시 반영된다.
- **발송 이력**: `tools/telegram/state.json` — 항목별 발송 횟수·다음 복습 예정일(에빙하우스 간격: 1·3·7·14·30일)을 자동 기록한다. 워크플로가 매 실행 후 스스로 커밋한다 — **손으로 편집하지 않는다**.
- **퀴즈 추가**: `tools/telegram/quiz-bank.json`에 `{id, sourceFile, question, answer(O/X), explanation}` 형식으로 추가. `sourceFile`은 실제 존재하는 판례/용어 md 경로여야 한다(발송 시 그 파일의 메타·링크를 읽어온다).
- **비밀키**: `TELEGRAM_TOKEN`·`TELEGRAM_CHAT_ID`는 GitHub 저장소의 **Actions Secrets**에만 있다. 어떤 파일에도 하드코딩하지 않는다.
- **수동 테스트**: GitHub의 Actions 탭 → 「텔레그램 공부 알리미」 → Run workflow → force=true로 즉시 1건 발송해 볼 수 있다. 로컬 테스트는 `TELEGRAM_TOKEN=... TELEGRAM_CHAT_ID=... FORCE_SEND=true node tools/telegram/send.mjs`.

## 기타 규칙

- 커밋 메시지는 한국어 평서문 「〜다.」 체로 작성
- 한글 파일명은 NFC/NFD 정규화가 섞여 있을 수 있음 — 빌드 스크립트는 정규화 비교로 처리한다
